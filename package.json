{
  "name": "the-optimist",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch": "tsx scripts/fetch.ts",
    "jobs": "tsx scripts/jobs.ts",
    "rewrite:api": "tsx scripts/rewrite-api.ts",
    "check": "tsx scripts/check.ts",
    "assemble": "tsx scripts/assemble.ts",
    "test": "node --import tsx --test scripts/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json && astro check",
    "results:parts": "tsx scripts/results-from-parts.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@guardian/source": "^13.1.0",
    "astro": "^7.3.4",
    "cheerio": "^1.2.0",
    "zod": "^4.6.5"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^6.0.3"
  }
}
